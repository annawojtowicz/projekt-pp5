<li>
    <a href="{{ path('app_car', {id:car.id}) }}"
       style="display:block; width:300px; height:300px; background-image:url(/{{ car.getWebPath() }});
    background-repeat: no-repeat;
               background-position:center center;
    background-size: contain;"
    ></a>
    <h4 class="username text-ellipsis">{{ car.brand }} <small>{{ car.model }}</small></h4>
</li>